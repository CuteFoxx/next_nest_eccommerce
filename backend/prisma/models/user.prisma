model User {
    id           Int      @id @default(autoincrement())
    username     String
    email        String   @unique
    password     String
    createdAt    DateTime @default(now())
    role         Role     @default(USER)
    refreshToken String?

    files File[]
}

enum Role {
    USER
    ADMIN
}
